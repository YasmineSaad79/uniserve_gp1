<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- 🌈 الثيم اللي بيظهر أثناء تشغيل التطبيق -->
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>

    <!-- 🌈 الثيم العام لتطبيق Flutter -->
    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">?android:colorBackground</item>
    </style>

    <!-- 🟣 الثيم الخاص بشاشة القص ImageCropper -->
    <style name="CropperTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- لون الشريط البنفسجي -->
        <item name="android:statusBarColor">#6A1B9A</item>
        <!-- لون شريط التنقل السفلي -->
        <item name="android:navigationBarColor">#000000</item>
        <!-- تجنّب التداخل مع شريط الحالة -->
        <item name="android:windowTranslucentStatus">false</item>
        <item name="android:fitsSystemWindows">true</item>
        <!-- دعم الـ notch (الثقب) بدون إخفاء الأزرار -->
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>

</resources>
